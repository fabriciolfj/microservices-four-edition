server.port: 7003
logging:
  level:
    root: INFO
    se.magnus: DEBUG
spring:
  web:
    error:
      include-message: always

---
spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8080
